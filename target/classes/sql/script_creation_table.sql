-- Création des tables
CREATE TABLE VILLE (ID int NOT NULL PRIMARY KEY, NOM varchar(100) NOT NULL, POPULATION int NOT NULL, ID_DEP varchar(5) NOT NULL, ID_REG int NOT NULL);
CREATE TABLE DEPARTEMENT (ID varchar(5) NOT NULL PRIMARY KEY);
CREATE TABLE REGION (ID int NOT NULL PRIMARY KEY, NOM varchar(100) NOT NULL UNIQUE);

-- Ajout des contraintes type « clés étrangères)
ALTER TABLE VILLE add constraint FK_VILLE_DEP FOREIGN KEY  (ID_DEP) REFERENCES DEPARTEMENT(ID) ;
ALTER TABLE VILLE add constraint FK_VILLE_REG FOREIGN KEY (ID_REG) REFERENCES REGION(ID) ;
